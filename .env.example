# ClipNest 可选环境变量（不配置也能跑，会用默认值）
# 真实密钥由程序首次启动时生成并写入 data/config.json，不放这里。

# 数据目录（默认为项目根下的 data/）
CLIPNEST_DATA_DIR=data

# 监听地址与端口
CLIPNEST_HOST=127.0.0.1
CLIPNEST_PORT=8420

# 初始管理员用户名（默认 1220）
CLIPNEST_ADMIN_USERNAME=1220

# JWT 有效期（天）
CLIPNEST_TOKEN_DAYS=30
